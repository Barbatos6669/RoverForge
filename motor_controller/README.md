This folder contains the Arduino sketch for RoverForge. Use the root Makefile to compile and upload.

Files:
- `motor_controller.ino` — main sketch
- `Makefile` — convenience targets: compile, upload, monitor

Quick commands:

```bash
make -C motor_controller compile
make -C motor_controller upload
make -C motor_controller monitor
```
